// Use a relative URL for production, or a full URL for local development if needed
export const API_URL = '';

// Default platform settings
export const DEFAULT_PLATFORM_FEE = 0.005; // 0.5%

// Pagination limits
export const DEFAULT_PAGE_SIZE = 10;

// Feature flags
export const FEATURES = {
  DICE_GAME_ENABLED: true,
  ESCROW_ENABLED: true,
  ANALYTICS_ENABLED: true
}; 
